# This work is provided "AS IS" and subject to the Shotgun Pipeline Toolkit 
# Source Code License included in this distribution package. See LICENSE.

# Metadata defining the behaviour and requirements for this engine

# expected fields in the configuration file for this engine
configuration:

# this app works in all engines - it does not contain 
# any host application specific commands
supported_engines: 

# the Shotgun fields that this engine needs in order to operate correctly
requires_shotgun_fields:

# More verbose description of this item 
display_name: "Multi Publish Dependencies"
description: "App to collect used not published dependencies and publish it."

# Required minimum versions for this item to run
requires_shotgun_version:
requires_core_version: "v0.14.28"
requires_engine_version:

# the frameworks required to run this app
frameworks:
    - {"name": "tk-framework-qtwidgets", "version": "v1.0.3"}
    - {"name": "tk-framework-shotgunutils", "version": "v4.4.15"}